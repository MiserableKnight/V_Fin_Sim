# 📘 越捷航空油价冲击财务决策模型升级指导方案
**从“财务测算工具”到“经营动作触发框架”落地指南**

---

## 🎯 一、 核心定位与战略升级方向
当前模型已具备完整的财务测算骨架（收入-变动成本-固定成本-套保-弹性-网络权重），能准确回答**“给定输入下，利润是多少、该削多少班”**。  
《意见一.md》的核心诉求是将其升级为**“油价冲击 → 经营动作触发路径”**。这不是推翻重来，而是**逻辑层的升维**：

| 维度         | 当前模型（V1）                  | 升级目标（V2）                                  |
| :----------- | :------------------------------ | :---------------------------------------------- |
| **交互逻辑** | 用户手动试错（调滑块看结果）    | 模型反推最优动作序列（输入冲击 → 输出触发路径） |
| **核心输出** | 利润值、削班比例、状态标签      | 阈值边界、动作优先级、约束失效预警              |
| **决策属性** | 静态 `What-If` 计算器           | 动态 `Trigger-Based` 决策支持系统               |
| **时间尺度** | 混合（票价/套保/削班/退租同层） | 分层（短期商业/金融 → 中期运力 → 长期机队）     |

**升级原则**：保持向后兼容，分阶段交付，先做“阈值+路径”（1-3天），再做“约束优化器”（1-2周），最后做“网络/机队解耦”（1-2个月）。

---

## 🔍 二、 关键逻辑辨析：为什么“先票价/套保 → 后削班”？边界在哪？
### 1. 行业惯例的底层经济学逻辑
航空业短期成本结构呈**“高固定、高沉没、低边际”**特征。动作优先级的本质是**“灵活性 vs 战略代价”**的权衡：

| 动作     | 调整周期        | 可逆性               | 对固定成本影响                     | 战略代价                           |
| :------- | :-------------- | :------------------- | :--------------------------------- | :--------------------------------- |
| 票价调整 | 分钟级（RMS）   | 极高                 | 无                                 | 可能流失价格敏感客群               |
| 套期保值 | 天/周级（财务） | 中（平仓有滑点）     | 无                                 | 油价下跌时产生机会成本             |
| 削减航班 | 周/月级（运控） | 极低（时刻可能丧失） | 几乎不降（租赁/折旧/核心人工照付） | 网络断裂、枢纽削弱、市占率永久丢失 |

**核心直觉**：只要 `边际贡献(CM) > 0`，每多飞一班就在为固定成本“续命”。削班等于主动切断正现金流，且固定成本不会同比例下降。因此，**在运力还能产生正边际贡献时，优先用商业/金融工具对冲，是保留运营期权价值的最优解。**

### 2. 为什么“不一定成立”？失效边界清单
该顺序是**条件最优经验法则**，非物理定律。以下场景会直接颠覆该逻辑：
- 🚨 **价格战/需求极度弹性**：竞品不跟涨，涨价导致需求断崖，收入跌幅 > 成本节省
- 🚨 **监管限价或长协锁定**：票价受政府指导价/企业协议约束，无法上调
- 🚨 **套保政策硬性上限**：航司风控规定套保覆盖率 ≤50%，且不可临时突破
- 🚨 **流动性危机**：即使 CM>0，但应收账期拉长/融资断裂，现金周转不灵
- 🚨 **冲击本质是需求萎缩**：油价上涨伴随经济衰退/疫情，涨价加速需求崩塌

**落地结论**：模型不应硬编码固定流程，而应建模为 **`带约束的优先级队列 + 阈值触发器`**。

---

## 🛠️ 三、 分阶段落地实施方案（详细步骤）

### ✅ Phase 1：阈值计算与决策路径可视化（1-3天，代码改动小，价值立现）

**目标**：在现有 `app.py` 中增加”智能推荐模式”，输出管理层最易用的阈值边界与阶梯响应路径。

**核心设计原则**：
- **单一职责**：阈值计算、决策路径构建、结果展示分离
- **可配置性**：硬编码系数提取为配置参数
- **容错性**：完整的边界检查和异常处理
- **可扩展性**：为后续蒙特卡洛模块预留接口

---

#### 步骤 1.1：新增阈值配置类（`threshold_config.py`）

```python
# -*- coding: utf-8 -*-
“””
油价冲击决策阈值配置模块

将原本硬编码的决策系数提取为可配置参数，
便于后续基于历史数据校准和情景调整。
“””

from dataclasses import dataclass, field
from typing import Dict, Tuple


@dataclass
class ThresholdConfig:
    “””决策阈值配置类”””

    # 决策阶段触发系数（基于油价容忍上限的百分比）
    STAGE_MONITOR_THRESHOLD: float = 0.60      # 监控阶段：油价 ≤ 容忍上限×60%
    STAGE_FARE_THRESHOLD: float = 0.85         # 票价阶段：油价 ≤ 容忍上限×85%
    STAGE_HEDGE_THRESHOLD: float = 1.00        # 套保阶段：油价 ≤ 容忍上限×100%

    # 动作约束
    FARE_ADJUST_MIN: float = -0.20             # 票价最小调整幅度（允许降价20%）
    FARE_ADJUST_MAX: float = 0.20              # 票价最大调整幅度
    HEDGE_RATIO_MIN: float = 0.0               # 最小套保比例
    HEDGE_RATIO_MAX: float = 0.8               # 最大套保比例

    # 求解器容差
    SOLVER_TOLERANCE: float = 1e-6
    SOLVER_MAX_ITER: int = 100

    # 决策路径描述模板
    ACTION_DESCRIPTIONS: Dict[str, str] = field(default_factory=lambda: {
        “monitor”: “边际贡献健康，保持商业监控”,
        “fare_increase”: “启动票价传导机制，逐步上调”,
        “hedge_position”: “同时动用票价与套保工具”,
        “capacity_reduction”: “商业/金融工具耗尽，触发运力压降”
    })

    def get_trigger_thresholds(self) -> Tuple[float, float, float]:
        “””返回三阶段触发阈值”””
        return (
            self.STAGE_MONITOR_THRESHOLD,
            self.STAGE_FARE_THRESHOLD,
            self.STAGE_HEDGE_THRESHOLD
        )


# 默认配置实例
DEFAULT_THRESHOLD_CONFIG = ThresholdConfig()
```

---

#### 步骤 1.2：在 `core_model.py` 新增增强版阈值解析器

```python
import numpy as np
from scipy.optimize import fsolve
from typing import Dict, List, Optional, Tuple
from dataclasses import dataclass

# 导入配置类
from threshold_config import DEFAULT_THRESHOLD_CONFIG


@dataclass
class ThresholdResult:
    “””阈值计算结果数据类”””
    fare_breakeven: float          # 票价盈亏平衡点（小数形式，如0.12表示+12%）
    oil_tolerance: float           # 最大可承受油价涨幅
    hedge_trigger: float           # 触发削班的临界套保比例
    current_stage: int             # 当前所处决策阶段（1-4）
    decision_path: List[Dict]      # 决策路径步骤列表
    warnings: List[str]            # 预警信息


class VietJetOilShockModel:
    “””越捷航空油价冲击财务决策模型（增强版）”””

    def __init__(self, config, threshold_config=None):
        “””
        Args:
            config: 原有的ModelConfig实例
            threshold_config: 阈值配置，默认使用DEFAULT_THRESHOLD_CONFIG
        “””
        self.config = config
        self.threshold_config = threshold_config or DEFAULT_THRESHOLD_CONFIG

    # ... (保留原有 __init__ 和 evaluate 方法) ...

    def get_thresholds(
        self,
        oil_increase: float = 0.0,
        fare_cap: Optional[float] = None,
        hedge_cap: Optional[float] = None,
        verbose: bool = False
    ) -> ThresholdResult:
        “””
        计算关键决策阈值与响应路径（Phase 1 核心）

        Args:
            oil_increase: 当前油价涨幅（小数形式，如0.4表示+40%）
            fare_cap: 票价调整上限，默认使用配置中的FARE_ADJUST_MAX
            hedge_cap: 套保比例上限，默认使用配置中的HEDGE_RATIO_MAX
            verbose: 是否输出详细计算过程

        Returns:
            ThresholdResult: 包含所有阈值和决策路径的结果对象

        Raises:
            ValueError: 当输入参数超出合理范围时
        “””
        # 参数校验
        if oil_increase < 0:
            raise ValueError(f”油价涨幅不能为负数: {oil_increase}”)

        # 使用默认配置或传入的约束
        fare_cap = fare_cap or self.threshold_config.FARE_ADJUST_MAX
        hedge_cap = hedge_cap or self.threshold_config.HEDGE_RATIO_MAX

        # 提取基础参数
        V_f = self.config.V_fuel_base      # 基础燃油成本
        V_o = self.config.V_other_base     # 其他变动成本
        R = self.config.R_base             # 基础收入
        FC = self.config.FC                # 固定成本
        eps = self.config.demand_elasticity  # 需求价格弹性

        warnings = []

        # ===== 阈值1：票价盈亏平衡点（CM=0，无套保保护） =====
        fare_breakeven = self._solve_fare_breakeven(
            oil_increase, R, V_o, V_f, eps
        )

        # 检查盈亏平衡点是否超过票价上限
        if fare_breakeven > fare_cap:
            warnings.append(
                f”⚠️ 票价盈亏平衡点(+{fare_breakeven*100:.1f}%) “
                f”超过设定上限(+{fare_cap*100:.1f}%)，需动用套保或削班”
            )

        # ===== 阈值2：最大可承受油价涨幅 =====
        oil_tolerance = self._calculate_oil_tolerance(
            R, V_o, V_f, fare_cap, hedge_cap, eps
        )

        if oil_increase > oil_tolerance:
            warnings.append(
                f”🚨 当前油价涨幅(+{oil_increase*100:.1f}%) “
                f”已超过理论容忍上限(+{oil_tolerance*100:.1f}%)”
            )

        # ===== 阈值3：触发削班的临界套保比例 =====
        hedge_trigger = self._solve_hedge_trigger(
            oil_increase, R, V_o, V_f, FC, fare_cap
        )

        if hedge_trigger > hedge_cap:
            warnings.append(
                f”🚨 避免削班所需套保比例({hedge_trigger*100:.1f}%) “
                f”超过政策上限({hedge_cap*100:.1f}%)，削班不可避免”
            )

        # ===== 构建决策路径 =====
        decision_path, current_stage = self._build_decision_path(
            oil_increase, oil_tolerance, fare_breakeven, hedge_trigger, fare_cap
        )

        if verbose:
            print(f”[阈值计算] 油价涨幅: {oil_increase*100:.1f}%”)
            print(f”[阈值计算] 盈亏平衡票价: {fare_breakeven*100:.1f}%”)
            print(f”[阈值计算] 油价容忍上限: {oil_tolerance*100:.1f}%”)
            print(f”[阈值计算] 削班触发套保线: {hedge_trigger*100:.1f}%”)

        return ThresholdResult(
            fare_breakeven=fare_breakeven,
            oil_tolerance=oil_tolerance,
            hedge_trigger=hedge_trigger,
            current_stage=current_stage,
            decision_path=decision_path,
            warnings=warnings
        )

    def _solve_fare_breakeven(
        self,
        oil_increase: float,
        R: float,
        V_o: float,
        V_f: float,
        eps: float
    ) -> float:
        “””
        求解票价盈亏平衡点

        定义：CM = R*(1+f)*(1+eps*f) - V_o - V_f*(1+oil_increase) = 0
        求解：f = ?
        “””
        if oil_increase <= 0:
            return 0.0

        # 构建方程：CM(f) = 0
        def cm_zero_equation(f: float) -> float:
            return R * (1 + f) * (1 + eps * f) - V_o - V_f * (1 + oil_increase)

        # 使用scipy.optimize.fsolve求解
        try:
            fare_breakeven = fsolve(
                cm_zero_equation,
                x0=0.1,  # 初始猜测值10%
                full_output=False
            )[0]
        except RuntimeError:
            # 求解失败，返回保守估计
            fare_breakeven = 0.15

        # 限制在合理区间
        min_fare, max_fare = self.threshold_config.FARE_ADJUST_MIN, self.threshold_config.FARE_ADJUST_MAX
        return np.clip(fare_breakeven, min_fare, max_fare)

    def _calculate_oil_tolerance(
        self,
        R: float,
        V_o: float,
        V_f: float,
        fare_cap: float,
        hedge_cap: float,
        eps: float
    ) -> float:
        “””
        计算最大可承受油价涨幅

        推导：
        调整后收入 = R * (1 + fare_cap) * (1 + eps * fare_cap)
        套保后燃油成本 = V_f * (1 + oil_increase * (1 - hedge_cap))
        盈亏平衡条件：调整后收入 = V_o + 套保后燃油成本
        “””
        # 调整后收入
        adjusted_revenue = R * (1 + fare_cap) * (1 + eps * fare_cap)

        # 计算油价容忍度
        # adjusted_revenue = V_o + V_f * (1 + oil_tol * (1 - hedge_cap))
        # => oil_tol = (adjusted_revenue - V_o - V_f) / (V_f * (1 - hedge_cap))

        denominator = V_f * (1 - hedge_cap)

        if abs(denominator) < self.threshold_config.SOLVER_TOLERANCE:
            # 套保比例100%，燃油成本完全锁定
            return np.inf

        oil_tolerance = (adjusted_revenue - V_o - V_f) / denominator

        return max(oil_tolerance, 0.0)

    def _solve_hedge_trigger(
        self,
        oil_increase: float,
        R: float,
        V_o: float,
        V_f: float,
        FC: float,
        fare_cap: float
    ) -> float:
        “””
        求解触发削班的临界套保比例

        定义：CM_min = FC（边际贡献刚好覆盖固定成本）
        求解：h = ?
        “””
        if oil_increase <= 0:
            return 0.0

        # 调整后收入
        adjusted_revenue = R * (1 + fare_cap) * (1 + self.config.demand_elasticity * fare_cap)

        # 构建方程：CM(h) - FC = 0
        def hedge_trigger_equation(h: float) -> float:
            fuel_cost_hedged = V_f * (1 + oil_increase * (1 - h))
            return adjusted_revenue - V_o - fuel_cost_hedged - FC

        try:
            hedge_trigger = fsolve(
                hedge_trigger_equation,
                x0=0.3,  # 初始猜测值30%
                full_output=False
            )[0]
        except RuntimeError:
            hedge_trigger = 0.5

        # 限制在[0, 1]区间
        return np.clip(hedge_trigger, 0.0, 1.0)

    def _build_decision_path(
        self,
        oil_increase: float,
        oil_tolerance: float,
        fare_breakeven: float,
        hedge_trigger: float,
        fare_cap: float
    ) -> Tuple[List[Dict], int]:
        “””
        构建决策响应路径

        基于当前油价涨幅和计算出的阈值，生成阶梯式响应方案
        “””
        # 获取配置的触发阈值
        monitor_thresh, fare_thresh, hedge_thresh = self.threshold_config.get_trigger_thresholds()

        path = []
        current_stage = 1

        # 阶段1：监控区间
        if oil_increase <= oil_tolerance * monitor_thresh:
            current_stage = 1
            path.append({
                “stage”: 1,
                “action”: “监控”,
                “trigger”: f”油价涨幅 ≤ {oil_tolerance*100:.0f}%×{monitor_thresh:.0f}”,
                “trigger_value”: f”≤{oil_tolerance*monitor_thresh*100:.1f}%”,
                “desc”: self.threshold_config.ACTION_DESCRIPTIONS[“monitor”],
                “recommended_fare”: 0.0,
                “recommended_hedge”: 0.0,
                “urgency”: “低 🟢”
            })

        # 阶段2：票价传导区间
        elif oil_increase <= oil_tolerance * fare_thresh:
            current_stage = 2
            recommended_fare = min(fare_breakeven, fare_cap)
            path.append({
                “stage”: 2,
                “action”: “票价上调”,
                “trigger”: f”油价突破 {oil_tolerance*100:.0f}%×{monitor_thresh:.2f}”,
                “trigger_value”: f”>{oil_tolerance*monitor_thresh*100:.1f}%”,
                “desc”: (
                    f”建议票价上调至 {recommended_fare*100:.0f}% “
                    f”(目标盈亏平衡点: {fare_breakeven*100:.1f}%)”
                ),
                “recommended_fare”: recommended_fare,
                “recommended_hedge”: 0.0,
                “urgency”: “中 🟡”
            })

        # 阶段3：套保加仓区间
        elif oil_increase <= oil_tolerance * hedge_thresh:
            current_stage = 3
            recommended_fare = fare_cap  # 票价已触及上限
            path.append({
                “stage”: 3,
                “action”: “套保加仓 + 票价”,
                “trigger”: f”油价突破 {oil_tolerance*100:.0f}%×{fare_thresh:.2f}”,
                “trigger_value”: f”>{oil_tolerance*fare_thresh*100:.1f}%”,
                “desc”: (
                    f”套保需覆盖 ≥{hedge_trigger*100:.0f}%，”
                    f”票价已触及上限({fare_cap*100:.0f}%)”
                ),
                “recommended_fare”: recommended_fare,
                “recommended_hedge”: min(hedge_trigger, self.threshold_config.HEDGE_RATIO_MAX),
                “urgency”: “高 🟠”
            })

        # 阶段4：削班启动
        else:
            current_stage = 4
            path.append({
                “stage”: 4,
                “action”: “削班启动”,
                “trigger”: f”油价 > {oil_tolerance*100:.0f}%”,
                “trigger_value”: f”>{oil_tolerance*100:.1f}%”,
                “desc”: self.threshold_config.ACTION_DESCRIPTIONS[“capacity_reduction”],
                “recommended_fare”: fare_cap,
                “recommended_hedge”: self.threshold_config.HEDGE_RATIO_MAX,
                “urgency”: “紧急 🔴”
            })

        return path, current_stage
```

---

#### 步骤 1.3：在 `app.py` Tab1 增加增强智能模式

```python
# ============ Tab1：智能决策模式 ============

with st.container():
    st.subheader(“🤖 智能决策模式”)

    # 模式选择
    col_mode1, col_mode2 = st.columns([3, 1])
    with col_mode1:
        auto_mode = st.checkbox(
            “启用智能推荐”,
            value=True,
            help=”模型自动计算最优阈值与动作序列，替代手动滑块”
        )
    with col_mode2:
        show_details = st.checkbox(“显示详情”, value=False)

    # 输入参数（始终显示，但智能模式下会自动填充推荐值）
    col_input1, col_input2 = st.columns(2)
    with col_input1:
        oil_increase_input = st.slider(
            “油价涨幅 (%)”,
            min_value=0,
            max_value=100,
            value=40,
            step=5,
            help=”相对基准价格的涨幅百分比”
        ) / 100

    # 约束设置（可折叠）
    with st.expander(“⚙️ 决策约束设置（可选）”):
        col_con1, col_con2 = st.columns(2)
        with col_con1:
            fare_cap = st.slider(“票价上调上限 (%)”, 0, 30, 20, 5) / 100
        with col_con2:
            hedge_cap = st.slider(“套保额度上限 (%)”, 0, 100, 80, 10) / 100

# ============ 智能模式输出 ============

if auto_mode:
    # 计算阈值
    try:
        threshold_result = model.get_thresholds(
            oil_increase=oil_increase_input,
            fare_cap=fare_cap,
            hedge_cap=hedge_cap,
            verbose=show_details
        )

        # ===== 阈值看板 =====
        st.subheader(“🎯 决策阈值看板”)

        # 紧急程度指示器
        stage_colors = {
            1: “🟢”,
            2: “🟡”,
            3: “🟠”,
            4: “🔴”
        }
        stage_names = {
            1: “正常监控”,
            2: “票价传导”,
            3: “套保加仓”,
            4: “削班启动”
        }

        st.markdown(
            f”**当前状态**: {stage_colors[threshold_result.current_stage]} “
            f”**阶段{threshold_result.current_stage}** — {stage_names[threshold_result.current_stage]}”
        )

        # 三阈值卡片
        c1, c2, c3 = st.columns(3)

        with c1:
            st.metric(
                label=”票价盈亏平衡点”,
                value=f”+{threshold_result.fare_breakeven*100:.1f}%”,
                delta=None,
                help=”在此票价水平下边际贡献刚好为0”
            )

        with c2:
            st.metric(
                label=”油价容忍上限”,
                value=f”+{threshold_result.oil_tolerance*100:.1f}%”,
                delta=f”当前: +{oil_increase_input*100:.0f}%”,
                help=”在票价和套保都用足时能承受的最大油价涨幅”
            )

        with c3:
            st.metric(
                label=”削班触发套保线”,
                value=f”{threshold_result.hedge_trigger*100:.1f}%”,
                delta=None,
                help=”套保比例需达到此值才能避免削班”
            )

        # ===== 决策响应路径 =====
        st.subheader(“📈 决策响应路径”)

        for step in threshold_result.decision_path:
            # 根据紧急程度选择容器类型
            if step[“urgency”] == “低 🟢”:
                container = st.success
            elif step[“urgency”] == “中 🟡”:
                container = st.info
            elif step[“urgency”] == “高 🟠”:
                container = st.warning
            else:  # 紧急
                container = st.error

            with container():
                col_s1, col_s2 = st.columns([3, 1])
                with col_s1:
                    st.markdown(f”**阶段 {step['stage']}**: {step['action']}”)
                    st.caption(f”📍 触发条件: {step['trigger_value']}”)
                    st.caption(f”💡 建议: {step['desc']}”)
                with col_s2:
                    st.markdown(f”**{step['urgency']}**”)

        # ===== 预警信息 =====
        if threshold_result.warnings:
            st.subheader(“⚠️ 预警信息”)
            for warning in threshold_result.warnings:
                st.warning(warning)

        # ===== 推荐动作执行 =====
        st.subheader(“🎬 推荐动作预览”)

        if threshold_result.decision_path:
            recommended = threshold_result.decision_path[0]
            col_r1, col_r2, col_r3 = st.columns(3)

            with col_r1:
                st.metric(
                    “推荐票价调整”,
                    f”+{recommended['recommended_fare']*100:.1f}%”
                )

            with col_r2:
                st.metric(
                    “推荐套保比例”,
                    f”{recommended['recommended_hedge']*100:.1f}%”
                )

            with col_r3:
                # 执行一次推荐配置的计算
                preview_result = model.evaluate(
                    oil_increase_input,
                    recommended['recommended_fare'],
                    recommended['recommended_hedge']
                )
                st.metric(
                    “预期边际贡献”,
                    f”{preview_result.total_cm:,.1f} 亿₫”
                )

        # ===== 详细计算过程（可选） =====
        if show_details:
            with st.expander(“🔍 详细计算过程”):
                st.json({
                    “输入参数”: {
                        “油价涨幅”: f”{oil_increase_input*100:.1f}%”,
                        “票价上限”: f”{fare_cap*100:.1f}%”,
                        “套保上限”: f”{hedge_cap*100:.1f}%”
                    },
                    “计算结果”: {
                        “票价盈亏平衡”: f”{threshold_result.fare_breakeven*100:.2f}%”,
                        “油价容忍度”: f”{threshold_result.oil_tolerance*100:.2f}%”,
                        “套保触发线”: f”{threshold_result.hedge_trigger*100:.2f}%”
                    },
                    “决策路径”: threshold_result.decision_path
                })

    except ValueError as e:
        st.error(f”❌ 参数错误: {str(e)}”)
    except Exception as e:
        st.error(f”❌ 计算失败: {str(e)}”)

else:
    # ===== 手动模式（原有逻辑） =====
    st.info(“👆 请关闭智能推荐以使用手动模式”)

    col_manual1, col_manual2, col_manual3 = st.columns(3)
    with col_manual1:
        fare_adj_manual = st.slider(“票价调整 (%)”, -20, 30, 0, 5) / 100
    with col_manual2:
        hedge_ratio_manual = st.slider(“套保比例 (%)”, 0, 100, 0, 10) / 100

    # 计算按钮
    if st.button(“🔄 计算”, type=”primary”):
        result = model.evaluate(oil_increase_input, fare_adj_manual, hedge_ratio_manual)

        # 显示结果（原有代码）
        # ...
```

---

### 📋 Phase 1 优化要点总结

| 维度 | 原方案 | 优化后 |
|:---|:---|:---|
| **代码组织** | 单一文件混合逻辑 | 配置类、数据类、模型类分离 |
| **硬编码系数** | 分散在各处 | 集中在 `ThresholdConfig` |
| **类型安全** | 无类型提示 | 完整的 `type hints` |
| **错误处理** | 无 | 参数校验 + 异常捕获 |
| **可扩展性** | 有限 | 预留蒙特卡洛接口 |
| **用户体验** | 基础展示 | 紧急度分级 + 详情展开 |

---

### ⚙️ Phase 2：约束型动作序列优化器（1-2周，改变模型调用逻辑）

**目标**：将 `输入(油价, 票价, 套保) → 输出(利润, 削班)` 升级为 `输入(油价, 约束) → 内部优化器 → 输出(动作序列, 触发阈值)`。

**核心设计原则**：
- **分层优化**：票价 → 套保 → 削班，依次激活
- **约束满足**：所有动作都在配置的上下界内
- **可解释性**：每个动作附带推荐理由

---

#### 步骤 2.1：新增优化结果数据类（`optimizer_results.py`）

```python
# -*- coding: utf-8 -*-
"""
动作序列优化结果数据结构
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional
from enum import Enum


class ActionStatus(Enum):
    """动作状态枚举"""
    OPTIMIZED = "optimized"       # 已优化到最优值
    SKIPPED = "skipped"           # 跳过（无需执行）
    TRIGGERED = "triggered"       # 已触发
    NOT_TRIGGERED = "not_triggered"  # 未触发
    CAP_EXCEEDED = "cap_exceeded"  # 触及上限


class ActionType(Enum):
    """动作类型枚举"""
    FARE_ADJUSTMENT = "fare"      # 票价调整
    HEDGE_POSITION = "hedge"      # 套保建仓
    CAPACITY_REDUCTION = "cut"    # 削班


@dataclass
class ActionStep:
    """单个动作步骤"""
    type: ActionType
    value: float                  # 动作值（如票价+0.12，套保0.35）
    status: ActionStatus
    description: str              # 动作描述
    rationale: str                # 推荐理由
    priority: int = 1             # 优先级（1=最高）


@dataclass
class OptimizationResult:
    """优化结果总览"""
    triggered_by: str             # 触发原因（如"油价+40%"）
    actions: List[ActionStep]     # 动作序列（按优先级排序）
    final_cm: float               # 最终边际贡献
    final_pnl: float              # 最终预期利润
    constraints_violated: List[str]  # 违反的约束列表
    is_feasible: bool             # 是否找到可行解
    optimization_summary: Dict[str, any] = field(default_factory=dict)
```

---

#### 步骤 2.2：在 `core_model.py` 新增增强版优化器

```python
import numpy as np
from typing import Dict, List, Optional, Tuple
from dataclasses import dataclass

# 导入结果数据类
from optimizer_results import (
    OptimizationResult, ActionStep, ActionType, ActionStatus
)


class VietJetOilShockModel:
    """越捷航空油价冲击财务决策模型（增强版）"""

    # ... (保留原有方法和Phase 1新增的方法) ...

    def recommend_action_sequence(
        self,
        oil_increase: float,
        fare_cap: Optional[float] = None,
        hedge_cap: Optional[float] = None,
        liquidity_buffer: float = 0.0,
        optimization_method: str = "grid",
        grid_resolution: int = 50
    ) -> OptimizationResult:
        """
        约束型动作序列优化器（Phase 2 核心）

        在给定约束下，按优先级依次优化票价、套保、削班动作，
        输出最优决策序列和推荐理由。

        Args:
            oil_increase: 油价涨幅（小数形式）
            fare_cap: 票价调整上限，默认使用配置值
            hedge_cap: 套保比例上限，默认使用配置值
            liquidity_buffer: 流动性缓冲（亿₫），用于现金压力测试
            optimization_method: 优化方法 ("grid"网格搜索 | "gradient"梯度法)
            grid_resolution: 网格搜索分辨率

        Returns:
            OptimizationResult: 优化结果对象

        Raises:
            ValueError: 当输入参数无效时
        """
        # 参数校验
        if oil_increase < 0:
            raise ValueError(f"油价涨幅不能为负: {oil_increase}")

        # 使用默认约束
        fare_cap = fare_cap or self.threshold_config.FARE_ADJUST_MAX
        hedge_cap = hedge_cap or self.threshold_config.HEDGE_RATIO_MAX

        # 提取基础参数
        V_f = self.config.V_fuel_base
        V_o = self.config.V_other_base
        R = self.config.R_base
        FC = self.config.FC
        eps = self.config.demand_elasticity

        actions: List[ActionStep] = []
        constraints_violated: List[str] = []

        # ===== Step 1: 票价寻优（最大化边际贡献） =====
        best_fare, fare_rationale = self._optimize_fare(
            oil_increase, fare_cap, R, V_o, V_f, eps,
            method=optimization_method, resolution=grid_resolution
        )

        actions.append(ActionStep(
            type=ActionType.FARE_ADJUSTMENT,
            value=best_fare,
            status=ActionStatus.OPTIMIZED if best_fare > 0 else ActionStatus.SKIPPED,
            description=f"票价调整 {best_fare*100:+.1f}%",
            rationale=fare_rationale,
            priority=1
        ))

        # 计算票价调整后的边际贡献
        cm_after_fare = self._calculate_cm(
            R, V_o, V_f, oil_increase, best_fare, eps, hedge_ratio=0.0
        )

        # ===== Step 2: 套保寻优（若CM仍不足覆盖FC） =====
        best_hedge = 0.0
        hedge_status = ActionStatus.SKIPPED
        hedge_rationale = "票价调整后CM已覆盖固定成本，无需套保"

        if cm_after_fare < FC:
            best_hedge, hedge_rationale = self._optimize_hedge(
                oil_increase, best_fare, hedge_cap, R, V_o, V_f, FC, eps
            )
            hedge_status = ActionStatus.OPTIMIZED if best_hedge > 0 else ActionStatus.SKIPPED

            # 检查是否触及上限
            if best_hedge >= hedge_cap and cm_after_fare < FC:
                hedge_status = ActionStatus.CAP_EXCEEDED
                constraints_violated.append("套保比例触及上限，无法完全对冲")

        actions.append(ActionStep(
            type=ActionType.HEDGE_POSITION,
            value=best_hedge,
            status=hedge_status,
            description=f"套保比例 {best_hedge*100:.1f}%",
            rationale=hedge_rationale,
            priority=2
        ))

        # 计算套保后的边际贡献
        cm_after_hedge = self._calculate_cm(
            R, V_o, V_f, oil_increase, best_fare, eps, hedge_ratio=best_hedge
        )

        # ===== Step 3: 削班判断（若CM仍为负或不足） =====
        cut_ratio, cut_status, cut_rationale = self._determine_cut_ratio(
            cm_after_hedge, FC
        )

        actions.append(ActionStep(
            type=ActionType.CAPACITY_REDUCTION,
            value=cut_ratio,
            status=cut_status,
            description=f"削减航班 {cut_ratio*100:.1f}%" if cut_ratio > 0 else "保持运力",
            rationale=cut_rationale,
            priority=3
        ))

        # 计算最终利润
        final_pnl = cm_after_hedge * (1 - cut_ratio) - FC

        # 判断是否找到可行解
        is_feasible = len(constraints_violated) == 0 and cm_after_hedge > 0

        return OptimizationResult(
            triggered_by=f"油价+{oil_increase*100:.0f}%",
            actions=actions,
            final_cm=cm_after_hedge,
            final_pnl=final_pnl,
            constraints_violated=constraints_violated,
            is_feasible=is_feasible,
            optimization_summary={
                "oil_increase": f"+{oil_increase*100:.1f}%",
                "cm_after_fare": cm_after_fare,
                "cm_after_hedge": cm_after_hedge,
                "fc_coverage": cm_after_hedge / FC if FC > 0 else float('inf')
            }
        )

    def _optimize_fare(
        self,
        oil_increase: float,
        fare_cap: float,
        R: float,
        V_o: float,
        V_f: float,
        eps: float,
        method: str = "grid",
        resolution: int = 50
    ) -> Tuple[float, str]:
        """
        票价寻优：在约束内寻找使边际贡献最大的票价调整幅度

        目标函数：max CM(f) = R*(1+f)*(1+eps*f) - V_o - V_f*(1+oil)
        约束：0 ≤ f ≤ fare_cap
        """
        if method == "grid":
            # 网格搜索
            best_fare = 0.0
            best_cm = float('-inf')

            for f in np.linspace(0, fare_cap, resolution):
                cm = self._calculate_cm(R, V_o, V_f, oil_increase, f, eps, 0.0)
                if cm > best_cm:
                    best_cm = cm
                    best_fare = f

        elif method == "gradient":
            # 梯度法（利用二次函数性质可直接求极值）
            # CM(f) = R*(1+f)*(1+eps*f) - constant
            #      = R*(1 + (1+eps)f + eps*f^2) - constant
            # dCM/df = R*((1+eps) + 2*eps*f) = 0
            # f* = -(1+eps) / (2*eps)
            if eps < -1e-6:  # 弹性为负
                f_star = -(1 + eps) / (2 * eps)
                best_fare = np.clip(f_star, 0, fare_cap)
            else:
                # 弹性非负时，单调关系
                best_fare = fare_cap if (1 + eps) > 0 else 0
        else:
            best_fare = 0.0

        # 生成推荐理由
        cm_at_best = self._calculate_cm(R, V_o, V_f, oil_increase, best_fare, eps, 0.0)
        cm_at_zero = self._calculate_cm(R, V_o, V_f, oil_increase, 0.0, eps, 0.0)

        if best_fare > 0:
            rationale = (
                f"油价冲击下，最优票价调整+{best_fare*100:.1f}%，"
                f"使边际贡献从{cm_at_zero:.1f}提升至{cm_at_best:.1f}亿₫"
            )
        else:
            rationale = "当前油价下，维持原票价可使边际贡献最大化"

        return best_fare, rationale

    def _optimize_hedge(
        self,
        oil_increase: float,
        fare_adj: float,
        hedge_cap: float,
        R: float,
        V_o: float,
        V_f: float,
        FC: float,
        eps: float
    ) -> Tuple[float, str]:
        """
        套保寻优：计算覆盖固定成本所需的最小套保比例

        目标：CM(h) ≥ FC
        约束：0 ≤ h ≤ hedge_cap

        推导：
        CM(h) = R_adj - V_o - V_f*(1 + oil*(1-h))
        设 CM(h) = FC，解出 h
        """
        # 计算调整后收入
        R_adj = R * (1 + fare_adj) * (1 + eps * fare_adj)

        # 当前CM（无套保）
        cm_no_hedge = R_adj - V_o - V_f * (1 + oil_increase)

        if cm_no_hedge >= FC:
            return 0.0, "当前CM已覆盖FC，无需套保"

        # 计算所需套保比例
        # R_adj - V_o - V_f*(1 + oil*(1-h)) = FC
        # => h = 1 - (R_adj - V_o - V_f - FC) / (V_f * oil_increase)

        numerator = R_adj - V_o - V_f - FC
        denominator = V_f * oil_increase

        if abs(denominator) < 1e-10:
            return 0.0, "油价无变化，无需套保"

        needed_hedge = 1 - numerator / denominator
        needed_hedge = np.clip(needed_hedge, 0.0, hedge_cap)

        # 生成推荐理由
        cm_at_hedge = R_adj - V_o - V_f * (1 + oil_increase * (1 - needed_hedge))
        fc_coverage = cm_at_hedge / FC if FC > 0 else 0

        if needed_hedge >= hedge_cap:
            rationale = (
                f"即使套保达到上限{hedge_cap*100:.0f}%，"
                f"也只能覆盖FC的{fc_coverage*100:.1f}%，需考虑削班"
            )
        else:
            rationale = (
                f"套保{needed_hedge*100:.1f}%可使CM达到{cm_at_hedge:.1f}亿₫，"
                f"覆盖FC的{fc_coverage*100:.1f}%"
            )

        return needed_hedge, rationale

    def _determine_cut_ratio(
        self,
        cm: float,
        FC: float
    ) -> Tuple[float, ActionStatus, str]:
        """
        削班判断：根据边际贡献与固定成本的关系确定削减比例

        分级逻辑：
        - CM ≤ 0: 重度削减（90%）
        - 0 < CM < FC: 轻度削减（按比例）
        - CM ≥ FC: 不削减
        """
        network_preserve = self.config.network_preserve

        if cm <= 0:
            # 边际贡献为负，大幅削减
            cut_ratio = 0.90
            status = ActionStatus.TRIGGERED
            rationale = (
                f"边际贡献为负({cm:.1f}亿₫)，"
                f"每飞一班亏损加剧，建议削减90%运力"
            )

        elif cm < FC:
            # 边际贡献不足覆盖固定成本，按比例削减
            # preserve = (CM/FC) * (1 - network_preserve) + network_preserve
            # 保留更多核心航线，削减边缘航线
            preserve_ratio = (cm / FC) * (1 - network_preserve) + network_preserve
            cut_ratio = max(0.05, min(0.80, 1.0 - preserve_ratio))

            status = ActionStatus.TRIGGERED
            rationale = (
                f"边际贡献({cm:.1f}亿₫)仅覆盖FC的{cm/FC*100:.1f}%，"
                f"建议削减{cut_ratio*100:.1f}%运力以保核心网络"
            )

        else:
            # 边际贡献充足，不削减
            cut_ratio = 0.0
            status = ActionStatus.NOT_TRIGGERED
            rationale = (
                f"边际贡献({cm:.1f}亿₫)充足覆盖FC({FC:.1f}亿₫)，"
                f"无需削班"
            )

        return cut_ratio, status, rationale

    def _calculate_cm(
        self,
        R: float,
        V_o: float,
        V_f: float,
        oil_increase: float,
        fare_adj: float,
        eps: float,
        hedge_ratio: float
    ) -> float:
        """计算边际贡献的辅助方法"""
        revenue = R * (1 + fare_adj) * (1 + eps * fare_adj)
        fuel_cost = V_f * (1 + oil_increase * (1 - hedge_ratio))
        return revenue - V_o - fuel_cost
```

---

#### 步骤 2.3：App 端增强集成

```python
# ============ Tab2：动作序列优化器 ============

with st.tabs(["智能决策", "动作优化器", "情景对比"]):
    with st.tab("动作优化器"):

        st.subheader("🔢 约束型动作序列优化")

        # 输入区域
        col_in1, col_in2, col_in3 = st.columns(3)

        with col_in1:
            oil_shock = st.slider(
                "油价冲击 (%)",
                0, 100, 40, 5,
                help="相对基准价格的涨幅"
            ) / 100

        with col_in2:
            opt_method = st.selectbox(
                "优化方法",
                ["grid", "gradient"],
                help="grid: 网格搜索（稳健）| gradient: 解析解（快速）"
            )

        with col_in3:
            show_calc = st.checkbox("显示计算过程", value=False)

        # 约束面板
        with st.expander("🔒 动作约束设置", expanded=True):
            const_col1, const_col2, const_col3 = st.columns(3)

            with const_col1:
                fare_max = st.slider("票价上限 (%)", 5, 50, 20, 5) / 100
            with const_col2:
                hedge_max = st.slider("套保上限 (%)", 0, 100, 80, 10) / 100
            with const_col3:
                liq_buffer = st.number_input(
                    "流动性缓冲 (亿₫)",
                    min_value=0.0,
                    max_value=10000.0,
                    value=0.0,
                    step=100.0
                )

        # 计算按钮
        if st.button("🚀 计算最优动作序列", type="primary", use_container_width=True):
            with st.spinner("正在优化动作序列..."):

                try:
                    result = model.recommend_action_sequence(
                        oil_increase=oil_shock,
                        fare_cap=fare_max,
                        hedge_cap=hedge_max,
                        liquidity_buffer=liq_buffer,
                        optimization_method=opt_method
                    )

                    # ===== 可行性检查 =====
                    if not result.is_feasible:
                        st.error("⚠️ 未找到满足所有约束的可行解")

                    if result.constraints_violated:
                        st.warning("**约束违规:**")
                        for violation in result.constraints_violated:
                            st.markdown(f"- {violation}")

                    # ===== 触发原因 =====
                    st.info(f"**优化触发**: {result.triggered_by}")

                    # ===== 动作序列卡片 =====
                    st.subheader("📋 推荐动作序列")

                    for i, action in enumerate(result.actions, 1):
                        # 状态颜色映射
                        status_config = {
                            ActionStatus.OPTIMIZED: ("🟢", "已优化"),
                            ActionStatus.SKIPPED: ("⚪", "跳过"),
                            ActionStatus.TRIGGERED: ("🔴", "已触发"),
                            ActionStatus.NOT_TRIGGERED: ("⚪", "未触发"),
                            ActionStatus.CAP_EXCEEDED: ("🟠", "触及上限")
                        }

                        icon, status_text = status_config.get(
                            action.status,
                            ("⚪", "未知")
                        )

                        with st.container():
                            # 动作头部
                            col_a1, col_a2, col_a3 = st.columns([1, 3, 2])

                            with col_a1:
                                st.markdown(f"**{icon} 步骤{i}**")

                            with col_a2:
                                st.markdown(f"**{action.description}**")

                            with col_a3:
                                st.caption(f"状态: {status_text}")

                            # 推荐理由
                            st.caption(f"💡 {action.rationale}")

                            # 进度条可视化
                            if action.type == ActionType.FARE_ADJUSTMENT:
                                st.progress(
                                    (action.value / fare_max) if fare_max > 0 else 0,
                                    f"票价调整幅度: {action.value*100:.1f}%"
                                )
                            elif action.type == ActionType.HEDGE_POSITION:
                                st.progress(
                                    action.value,
                                    f"套保比例: {action.value*100:.1f}%"
                                )
                            elif action.type == ActionType.CAPACITY_REDUCTION:
                                if action.value > 0:
                                    st.progress(
                                        action.value,
                                        f"削减比例: {action.value*100:.1f}%"
                                    )
                                else:
                                    st.success("✅ 保持完整运力")

                            st.divider()

                    # ===== 最终结果摘要 =====
                    st.subheader("📊 优化结果摘要")

                    summary_col1, summary_col2, summary_col3 = st.columns(3)

                    with summary_col1:
                        st.metric(
                            "最终边际贡献",
                            f"{result.final_cm:,.1f} 亿₫",
                            help="执行所有动作后的预期边际贡献"
                        )

                    with summary_col2:
                        st.metric(
                            "预期利润",
                            f"{result.final_pnl:,.1f} 亿₫",
                            delta_color="normal"
                        )

                    with summary_col3:
                        fc_coverage = result.optimization_summary.get("fc_coverage", 0)
                        st.metric(
                            "固定成本覆盖",
                            f"{fc_coverage*100:.1f}%" if fc_coverage < float('inf') else "∞",
                            help="边际贡献占固定成本的比例"
                        )

                    # ===== 详细计算过程 =====
                    if show_calc:
                        with st.expander("🔍 优化计算详情"):
                            st.json({
                                "输入参数": {
                                    "油价冲击": f"{oil_shock*100:.1f}%",
                                    "票价上限": f"{fare_max*100:.1f}%",
                                    "套保上限": f"{hedge_max*100:.1f}%",
                                    "优化方法": opt_method
                                },
                                "优化过程": result.optimization_summary,
                                "动作详情": [
                                    {
                                        "类型": a.type.value,
                                        "值": a.value,
                                        "状态": a.status.value,
                                        "理由": a.rationale
                                    }
                                    for a in result.actions
                                ]
                            })

                except ValueError as e:
                    st.error(f"❌ 参数错误: {str(e)}")
                except Exception as e:
                    st.error(f"❌ 优化失败: {str(e)}")
```

---

### 📋 Phase 2 优化要点总结

| 维度 | 原方案 | 优化后 |
|:---|:---|:---|
| **数据结构** | 返回dict | 返回`OptimizationResult`数据类 |
| **动作封装** | 字典列表 | `ActionStep`类（含类型、状态、理由） |
| **寻优方法** | 仅网格搜索 | 网格 + 梯度法可选 |
| **错误处理** | 无 | 完整的参数校验和异常捕获 |
| **可解释性** | 简单理由 | 每步动作附带详细推荐理由 |
| **UI展示** | JSON输出 | 结构化卡片 + 进度条可视化 |

---

### 🌐 Phase 3：网络约束与中长期模块解耦（1-2个月，需数据支持）
**目标**：将 `network_preserve` 从财务修正项升级为航线级硬约束；明确短/中/长期决策边界。

| 模块               | 定位          | 输入数据                                       | 输出动作                          |
| :----------------- | :------------ | :--------------------------------------------- | :-------------------------------- |
| **短期（0-6月）**  | 商业/金融对冲 | 油价、竞品票价、RMS弹性、套保额度              | 调价、套保比例、临时削班          |
| **中期（6-18月）** | 运力结构优化  | 机型利用率、时刻表、租赁到期日、维修计划       | 机型调配、湿租/退租评估、航线重组 |
| **长期（18月+）**  | 机队/网络战略 | 采购订单、枢纽战略、碳税/ESG政策、宏观需求预测 | 机队更替、枢纽迁移、长期套保策略  |

**落地建议**：
1. 当前 V2 仅交付**短期模块**，在 README 明确标注边界。
2. 用 `route_tiers = {"core_hub": 0.40, "regional": 0.35, "leisure": 0.25}` 模拟网络约束，削班时按优先级分配削减量。
3. 为中长期预留 API 接口：`fleet_utilization_target`, `lease_expiry_schedule`。

---

## ⚠️ 四、 风险控制与模型校准建议
1. **防过度工程化**：阈值和优化器需定期用历史油价冲击期数据（如 2022Q2、2024Q3）回测校准。
2. **弹性动态化**：`demand_elasticity = -0.8` 是行业均值。实际应分航线/舱位设置弹性矩阵，或在 UI 提供“商务/休闲”切换开关。
3. **流动性红线**：模型默认关注利润，但航司危机常死于现金流。建议增加 `cash_flow_buffer` 参数，当 `expected_pnl < -buffer` 时直接触发 `critical` 状态。
4. **监管/竞品约束**：在 `app.py` 增加 `competitor_fare_follow` 复选框。若未勾选，票价上调幅度需乘以 `0.6~0.8` 衰减系数。

---

## 📎 五、 交付物清单与下一步行动
| 交付物                                                       | 状态           | 负责人/工具           |
| :----------------------------------------------------------- | :------------- | :-------------------- |
| `core_model.py` 阈值解析器 (`get_thresholds`)                | ✅ 可立即集成   | 复制上述 Phase 1 代码 |
| `core_model.py` 动作序列优化器 (`recommend_action_sequence`) | ✅ 可立即集成   | 复制上述 Phase 2 代码 |
| `app.py` 智能决策面板 UI                                     | ✅ 可立即集成   | 复制上述 App 代码段   |
| 《油价冲击-经营动作触发逻辑树》可视化图                      | 📝 待生成       | Plotly/Dagre 或 PPT   |
| 历史数据回测校准脚本                                         | 📅 Phase 3 规划 | 需财务/运控数据       |

### 🚀 立即执行步骤（今天即可完成）
1. 将 `get_thresholds()` 和 `_build_decision_path()` 粘贴至 `core_model.py` 类末尾。
2. 在 `app.py` Tab1 替换原有手动计算逻辑，加入 `auto_mode` 分支。
3. 运行 `streamlit run app.py`，测试 `油价=40%` 时的阈值输出与路径展示。
4. 截图阈值看板，替换 README 中的静态说明图。

如需，我可立即为你生成：
- 完整可运行的 `core_model.py` 与 `app.py` 补丁文件（含完整注释与类型提示）
- 符合研报标准的 `“油价冲击-经营动作触发逻辑树”`（PNG/SVG）
- 历史回测校准模板（Jupyter Notebook，含 2022/2024 真实油价序列）

请告知你优先需要哪一项，或指出上述方案中需调整的假设/参数。

































你说得非常对。**蒙特卡洛模拟确实是这个模型从"确定性测算"跃迁到"概率决策支持"的关键缺失环节。**

之前的方案聚焦在"阈值+动作序列"的逻辑重构，但所有阈值本质上都是**点估计**。而现实决策中，管理层真正需要的是：
> "在油价+40%的情景下，票价上调10%的策略，**有多大把握**能避免削班？"

下面给出蒙特卡洛模块的**价值定位、集成方案与落地代码**，确保与现有架构无缝衔接。

---

## 🎯 一、为什么需要蒙特卡洛？—— 从"单点答案"到"概率分布"

| 问题类型               | 确定性模型回答                            | 蒙特卡洛增强后回答                                           |
| :--------------------- | :---------------------------------------- | :----------------------------------------------------------- |
| 油价+40%时该不该涨价？ | "涨价10%后边际贡献=11.4万亿₫，建议不削班" | "涨价10%后，**78%的模拟情景**下边际贡献>0，**52%的概率**能覆盖固定成本" |
| 套保30%够不够？        | "套保30%时盈亏平衡点对应油价+52%"         | "套保30%时，**油价容忍度的90%置信区间**为[+38%, +67%]"       |
| 削班建议可靠吗？       | "建议削减30.1%航班"                       | "建议削减比例的中位数为30%，**80%概率落在[22%, 41%]区间**"   |

**核心价值**：把"模型输出一个数字"升级为"模型输出一个决策置信度"，让管理层知道"这个建议在多大程度上靠谱"。

---

## 🛠️ 二、蒙特卡洛模块集成方案（分阶段，兼容现有架构）

### ✅ Phase A：最小可行集成（1-2天，代码改动<50行）
**目标**：在现有 `evaluate()` 方法外包裹一层概率采样，输出关键指标的分布统计。

#### 步骤1：在 `core_model.py` 新增 `evaluate_monte_carlo` 方法
```python
import numpy as np
from scipy import stats
from typing import Dict, List

class VietJetOilShockModel:
    # ... (保留原有方法) ...

    def evaluate_monte_carlo(
        self,
        oil_increase: float,
        fare_adj: float = 0.0,
        hedge_ratio: float = 0.0,
        n_simulations: int = 5000,
        distributions: Optional[Dict[str, tuple]] = None
    ) -> Dict[str, any]:
        """
        蒙特卡洛模拟评估
        Args:
            distributions: 关键参数的概率分布配置，格式 {"param_name": (dist_type, params)}
                          示例: {"demand_elasticity": ("norm", (-0.8, 0.15))}
        """
        # 默认分布配置（可外部覆盖）
        default_dists = {
            "demand_elasticity": ("truncnorm", (-2.0, -0.3, -0.8, 0.15)),  # 截断正态，范围[-2,-0.3]
            "V_fuel_base": ("norm", (self.config.V_fuel_base, 0.05 * self.config.V_fuel_base)),  # ±5%波动
            "R_base": ("norm", (self.config.R_base, 0.03 * self.config.R_base)),  # ±3%波动
        }
        dists = {**default_dists, **(distributions or {})}
        
        # 采样器
        def sample_param(dist_spec):
            dist_type, params = dist_spec
            if dist_type == "norm":
                return np.random.normal(*params)
            elif dist_type == "truncnorm":
                # scipy.stats.truncnorm: a,b是标准正态下的截断点，loc,scale是实际均值和标准差
                a, b, loc, scale = params
                a_std = (a - loc) / scale
                b_std = (b - loc) / scale
                return stats.truncnorm.rvs(a_std, b_std, loc=loc, scale=scale)
            elif dist_type == "uniform":
                return np.random.uniform(*params)
            else:
                return params[0]  # 回退到确定值
        
        # 批量模拟
        results = []
        for _ in range(n_simulations):
            # 临时覆盖配置
            temp_config = ModelConfig(
                R_base=sample_param(dists.get("R_base", ("norm", (self.config.R_base, 0)))),
                V_fuel_base=sample_param(dists.get("V_fuel_base", ("norm", (self.config.V_fuel_base, 0)))),
                V_other_base=self.config.V_other_base,  # 暂设其他变动成本稳定
                FC=self.config.FC,  # 固定成本短期刚性，暂不设波动
                demand_elasticity=sample_param(dists.get("demand_elasticity", ("norm", (self.config.demand_elasticity, 0)))),
                network_preserve=self.config.network_preserve
            )
            temp_model = VietJetOilShockModel(temp_config)
            res = temp_model.evaluate(oil_increase, fare_adj, hedge_ratio)
            results.append({
                "total_cm": res.total_cm,
                "expected_pnl": res.expected_pnl,
                "cut_ratio": res.cut_ratio,
                "status_type": res.status_type
            })
        
        # 统计分析
        df_sim = pd.DataFrame(results)
        return {
            "point_estimate": self.evaluate(oil_increase, fare_adj, hedge_ratio),  # 保留原确定性结果
            "simulation_summary": {
                "n_simulations": n_simulations,
                "total_cm": {
                    "mean": df_sim["total_cm"].mean(),
                    "std": df_sim["total_cm"].std(),
                    "p5": df_sim["total_cm"].quantile(0.05),
                    "p50": df_sim["total_cm"].quantile(0.5),
                    "p95": df_sim["total_cm"].quantile(0.95)
                },
                "expected_pnl": {
                    "mean": df_sim["expected_pnl"].mean(),
                    "prob_positive": (df_sim["expected_pnl"] > 0).mean(),
                    "prob_cover_fc": (df_sim["total_cm"] >= self.config.FC).mean()
                },
                "cut_ratio": {
                    "median": df_sim["cut_ratio"].median(),
                    "p80_interval": (df_sim["cut_ratio"].quantile(0.1), df_sim["cut_ratio"].quantile(0.9))
                },
                "status_distribution": df_sim["status_type"].value_counts(normalize=True).to_dict()
            }
        }
```

#### 步骤2：在 `app.py` Tab1 增加"🎲 概率评估"开关
```python
# 在单情景分析区域添加：
mc_toggle = st.checkbox("🎲 启用蒙特卡洛概率评估", value=False, help="对关键参数引入波动，输出决策置信区间")

if mc_toggle:
    with st.spinner("正在运行5000次模拟..."):
        mc_result = model.evaluate_monte_carlo(oil_increase, fare_adj, hedge_ratio)
        
        st.subheader("📊 概率评估摘要")
        c1, c2, c3 = st.columns(3)
        c1.metric("盈利概率", f"{mc_result['simulation_summary']['expected_pnl']['prob_positive']*100:.1f}%")
        c2.metric("覆盖固定成本概率", f"{mc_result['simulation_summary']['expected_pnl']['prob_cover_fc']*100:.1f}%")
        c3.metric("建议削班比例(中位数)", f"{mc_result['simulation_summary']['cut_ratio']['median']*100:.1f}%")
        
        # 状态分布可视化
        status_dist = mc_result['simulation_summary']['status_distribution']
        st.bar_chart(pd.Series(status_dist).rename({"healthy":"✅健康","warning":"⚠️承压","critical":"🚨危险"}))
        
        # 边际贡献分布图
        fig = px.histogram(x=[r["total_cm"] for r in [model.evaluate_monte_carlo(oil_increase, fare_adj, hedge_ratio)["simulation_summary"]]*0], 
                          title="边际贡献分布（示意）", labels={"x":"总边际贡献(万亿₫)"})
        # 实际应传入模拟结果数组，此处简化
        st.plotly_chart(fig, use_container_width=True)
else:
    # 原有确定性计算逻辑
    result = model.evaluate(...)
```

---

### ⚙️ Phase B：阈值概率化 + 动作序列置信度（1周，进阶价值）
**目标**：将 Phase 1 的"阈值"升级为"阈值概率分布"，让动作触发逻辑也带置信度。

```python
def get_thresholds_probabilistic(self, oil_increase: float, n_sim: int = 3000) -> dict:
    """概率化阈值计算：输出阈值的置信区间而非单点"""
    thresholds_samples = []
    
    for _ in range(n_sim):
        # 采样关键参数
        eps_sample = np.random.normal(self.config.demand_elasticity, 0.15)
        V_f_sample = np.random.normal(self.config.V_fuel_base, 0.05 * self.config.V_fuel_base)
        
        # 解析求解盈亏平衡票价（同原get_thresholds逻辑，但用采样参数）
        # ... (略，复用原数学推导)
        fare_breakeven_sample = solve_fare_breakeven(eps_sample, V_f_sample, ...)
        thresholds_samples.append(fare_breakeven_sample)
    
    return {
        "fare_breakeven": {
            "point": np.median(thresholds_samples),
            "p5": np.percentile(thresholds_samples, 5),
            "p95": np.percentile(thresholds_samples, 95),
            "prob_exceeded": (np.array(thresholds_samples) < 0.15).mean()  # 涨价15%能覆盖盈亏平衡的概率
        },
        # 其他阈值同理...
    }
```

**UI呈现建议**：
```
🎯 票价盈亏平衡点：+12.3% 
   ├─ 90%置信区间：[+8.1%, +18.7%]
   └─ 若涨价上限=15%，则**73%概率**能避免边际贡献为负
```

---

### 🌐 Phase C：端到端决策路径概率图（中长期，需数据校准）
**目标**：输出"油价冲击 → 动作序列"的概率决策树，类似：
```
油价+40%
├─ 68%概率：票价上调≤15% + 套保30% → 无需削班
├─ 24%概率：需部分削班(中位数28%)
└─ 8%概率：触发战略停飞（需求弹性极端敏感情景）
```

**实现要点**：
1. 将 `recommend_action_sequence` 改造为返回概率分布
2. 用 `plotly` 绘制决策树，节点大小=概率，颜色=置信度
3. 支持"压力测试模式"：手动调整某个参数的波动范围，观察决策路径敏感性

---

## ⚠️ 三、实施注意事项（避免踩坑）

1. **性能优化**：
   - 蒙特卡洛计算量大，Streamlit 中务必加 `@st.cache_data` 缓存相同参数的模拟结果
   - 默认 `n_simulations=2000` 平衡精度与速度，高级用户可手动调高

2. **分布设定务实**：
   - 不要过度复杂化，初期聚焦 **需求弹性、基础收入、燃油成本** 三个核心波动源
   - 分布参数可来自：历史波动率、分析师预测区间、管理层主观判断（提供手动输入框）

3. **输出管理层友好**：
   - 避免直接输出"标准差=2.3"，改为"80%概率落在[11.2, 15.8]万亿₫"
   - 关键决策点用**概率+区间+建议**三要素呈现

4. **与现有逻辑兼容**：
   - 蒙特卡洛是"增强层"，不替换原有 `evaluate()`，用 `mode="deterministic" | "probabilistic"` 切换
   - 所有概率输出都附带"点估计"作为基准参考

---

## 📎 四、立即执行清单（今天可启动）

1. ✅ 将 `evaluate_monte_carlo()` 代码粘贴至 `core_model.py` 类末尾
2. ✅ 在 `app.py` Tab1 添加 `mc_toggle` 开关与概率摘要卡片
3. ✅ 测试运行：油价+40%/票价+8%/套保30%，观察"盈利概率"输出
4. ✅ 在 README 新增"🎲 概率评估模式"使用说明

如需，我可立即为你生成：
- 完整可运行的 `evaluate_monte_carlo()` 代码（含截断正态采样等细节）
- Streamlit 概率评估组件的完整 UI 代码（含分布可视化）
- 《蒙特卡洛参数校准指南》（如何用历史数据/专家判断设定分布）

请告诉我你优先需要哪一项，或指出上述方案中需调整的假设/参数。